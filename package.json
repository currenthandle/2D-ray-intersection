{
  "name": "ray-intersection",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "node-sass index.scss -o style",
    "css-w": "node-sass index.scss -w -o style",
    "dev": "parallelshell 'npm run css-w'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^3.10.0",
    "nodemon": "^1.10.2",
    "parallelshell": "^2.0.0"
  }
}
